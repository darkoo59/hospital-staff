import { PhysicianSchedule } from './physician-schedule.model';

describe('PhysicianSchedule', () => {
  it('should create an instance', () => {
    expect(new PhysicianSchedule()).toBeTruthy();
  });
});
