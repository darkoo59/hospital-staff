import { InpatientTreatment } from './inpatient-treatment.model';

describe('InpatientTreatment', () => {
  it('should create an instance', () => {
    expect(new InpatientTreatment()).toBeTruthy();
  });
});
